# Micro-Sniper canvas RPA server (crawl4ai)
PORT=1111
DEBUG=true
DATA_DIR=../data

# LLM provider for AI node-graph generation (crawl4ai LLMConfig)
LLM_PROVIDER=openai/gpt-4o-mini
OPENAI_API_KEY=

# Run-result retention / rotation
RESULTS_MAX_RUNS=200
RESULTS_MAX_MB=500
